var hash = window.location.hash;
	
	// scroll to div on nav click
	$("a").click(function (e) {
		var id = $(this).attr("href"); 
        $(window.location.hash).scrollView({offset: HeaderOffset});
		
	});
